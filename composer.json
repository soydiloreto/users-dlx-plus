{
    "name": "soydiloreto/users-plus-for-wordpress",
    "description": "Users Plus for WordPress WordPress plugin — Composer manifest for development tooling. Not used at runtime.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "homepage": "https://github.com/soydiloreto/users-plus-for-wordpress",
    "support": {
        "issues": "https://github.com/soydiloreto/users-plus-for-wordpress/issues",
        "source": "https://github.com/soydiloreto/users-plus-for-wordpress"
    },
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "brain/monkey": "^2.6",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "humanmade/psalm-plugin-wordpress": "^3.0",
        "mockery/mockery": "^1.6",
        "phpcompatibility/phpcompatibility-wp": "^2.1",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpstan": "^1.12",
        "phpunit/phpunit": "^9.6",
        "szepeviktor/phpstan-wordpress": "^1.3",
        "vimeo/psalm": "^5.26",
        "wp-coding-standards/wpcs": "^3.1"
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true,
        "platform": {
            "php": "7.4.30"
        },
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "phpstan/extension-installer": true
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:unit": "phpunit --testsuite unit",
        "lint": "phpcs",
        "lint:fix": "phpcbf",
        "stan": "phpstan analyse --memory-limit=2G",
        "psalm:taint": "psalm --taint-analysis --no-cache --no-progress"
    },
    "scripts-descriptions": {
        "test": "Run all PHPUnit suites.",
        "test:unit": "Run only the unit-test suite (no WordPress runtime required, fast).",
        "lint": "Run PHP_CodeSniffer with WordPress Coding Standards. Reports violations.",
        "lint:fix": "Run PHP Code Beautifier and Fixer to auto-fix violations PHPCBF can handle.",
        "stan": "Run PHPStan static analysis at the configured level (see phpstan.neon).",
        "psalm:taint": "Run Psalm in taint-analysis mode to flag user-controlled values flowing into dangerous sinks (XSS, SQLi, RCE)."
    }
}
